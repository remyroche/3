Flask>=2.0.0 # Or your specific Flask version
Flask-JWT-Extended>=4.0.0
Flask-CORS>=3.0.0
Pillow>=9.0.0
qrcode>=7.0
Unidecode>=1.0
python-dotenv>=0.19.0 # Recommended for managing environment variables
Flask-Talisman>=1.0.0 # For security headers
weasyprint
marshmallow>=3.0.0 # For input validation
pyotp>=2.6.0 # For TOTP
Flask-Limiter>=3.0.0 # Ensure you have this or a compatible version
bcrypt>=3.2.0 # If not already using werkzeug's hashing, bcrypt is a good alternative. Werkzeug is fine.
