// website/admin/js/admin_config.js

// Define the base URL for your backend API
const API_BASE_URL = 'http://localhost:5001'; // Base URL for all backend API calls
                                              // Ensure your backend server is running on this address and port.
// If you later decide to split your auth server or other admin APIs to different URLs,
// you can re-introduce specific constants like ADMIN_AUTH_BASE_URL.